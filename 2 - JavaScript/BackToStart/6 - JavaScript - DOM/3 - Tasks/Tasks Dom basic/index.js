/*
1. get an url of pic from the client (prompt) and show the pic in the dom.
2. use loop function  to show the same pict many times.
 */

// 1. 

// const userPic = prompt('Enter url of pic') // url of pic from user

// const body = document.querySelector('body') // the parent of the element

// function showPicFromUser () {

    // const img = document.createElement('img') // create tag for url from user
    // img.src = userPic // also you can use setAttribute() - to set attribute to the element.
    // console.log(img)
    
    // img.setAttribute( "src", userPic) 

    // console.log(img)

    // body.appendChild(img)
// }    

// showPicFromUser()


// 2.

// for (let i =0; i < 10; i++) {
//     const img = document.createElement('img')
//     img.src = userPic
//     console.log(img)

//     body.appendChild(img)
// }











